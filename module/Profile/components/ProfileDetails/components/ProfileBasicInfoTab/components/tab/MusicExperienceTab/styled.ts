import { Typography } from '@mui/material'
import { styled } from '@mui/system'

export const MusicExperienceTabContainer = styled('div')(() => ({
  display: 'flex',
  flexDirection: 'column',
  width: '100vw',
  height: 'auto',
}))

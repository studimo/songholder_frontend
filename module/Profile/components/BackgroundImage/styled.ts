import { Box, styled } from '@mui/material'

export const StyledCoverImage = styled(Box)(() => ({
  display: 'flex',
  position: 'relative',
  width: '100vw',
  height: '200px',
  overflow: 'hidden',
}))

import { Typography } from '@mui/material'
import { styled } from '@mui/system'

export const FollowerAndLikeContainer = styled('div')(() => ({
  display: 'flex',
  flexDirection: 'row',
  width: '100vw',
  height: 'auto',
  marginTop: '15px',
  justifyContent: 'center',
  alignItems: 'center',
}))
export const DividerDiv = styled('div')(() => ({
  display: 'flex',
  width: '2px',
  height: '30px',
  backgroundColor: 'rgba(162, 120, 188, 0.5)',
}))

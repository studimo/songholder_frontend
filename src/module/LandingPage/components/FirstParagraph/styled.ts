import { Box, Container, Divider } from '@mui/material'
import { styled } from '@mui/system'

export const FirstParagraphBox = styled(Box)(() => ({
  mixBlendMode: 'multiply',
  width: '100vw',
  marginTop: '-450px',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
}))

import { styled } from '@mui/system'

export const TimelineCardContainer = styled('div')(() => ({
  display: 'flex',
  flexDirection: 'row',
  paddingLeft: '25px',
  height: 'auto',
  minHeight: '60px',
}))
